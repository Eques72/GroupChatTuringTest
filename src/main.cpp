#include <iostream>
#include <cstdint>


auto main(int32_t argc, char * argv[]) -> int32_t
{
    std::cout << "Hello, World!" << std::endl;

/*
    So the uWebSocket is the only legit C++ weboscket library, even though it is kinda weird to use IMO.
    I can't really recommend other languages since: no java because, C# with ASP.NET is too much hassle IMO, I refuse to use javascript, I don't want to learn rust, ...
    Maybe Kotlin?!
*/

    return EXIT_SUCCESS;
}
